name: dpdrive

services:
  dpdrive:
    image: superneed/dpdrive:latest
    container_name: dpdrive
    restart: unless-stopped
    ports:
      - "18088:8088"
    volumes:
      - dpdrive-data:/app/data

volumes:
  dpdrive-data:
